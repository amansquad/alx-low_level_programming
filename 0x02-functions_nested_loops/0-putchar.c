#include <stdio.h>
#include "main.h"
/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(c)/*prints the alphabet in lowercase, followed by a new line.*/
{}
printf("_putchar\n");
return (0);
}
