#include <stdio.h>
/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)/*prints the alphabet in lowercase, followed by a new line.*/
{
char i = '0';
while (i <= '9')
{
putchar(i);
i++;
}
printf("\n");
return (0);
}
