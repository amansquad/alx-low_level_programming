#include "main.h"
/**
* mul -> this is a function to check upper case of alphabet
* @a: an integer parameter
* @b: an integer parameter
* Return: something but success
*/
int mul(int a, int b)
{
return (a * b);
}
