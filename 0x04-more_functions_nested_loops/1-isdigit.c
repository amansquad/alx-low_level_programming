#include "main.h"
/**
* _isdigit -> this is a function to check upper case of alphabet
* @c: an integer parameter
* Return: something but success
*/
int _isdigit(int c)
{
return (c <= '0' && c >= '9');
}
